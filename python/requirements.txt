# Зависимости проекта. Установка: pip install -r requirements.txt
# Источник версий — pyproject.toml, при добавлении пакета правьте оба файла.
pyswip>=0.2.10

# Инструменты разработки: тесты, линтер, pre-commit
pytest>=8
ruff>=0.5
pre-commit>=3

# Установка самого проекта в режиме разработки (чтобы работали импорты из src/)
-e .
